html
  head
    link(rel="stylesheet", href="/style.css")
  body
    .container
      .box
        if board
          .post-title #{board.title}
          .post-info 작성일: #{board.createdAt.toLocaleString()}
          .post-content #{board.content}

          a.btn-back(href="/board") ← 목록으로 돌아가기
        else
          h1 게시글을 찾을 수 없습니다.
          a.btn-back(href="/board") 목록으로
